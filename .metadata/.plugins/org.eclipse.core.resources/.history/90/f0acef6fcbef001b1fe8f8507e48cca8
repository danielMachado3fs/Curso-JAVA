package classes_metodos;

public class Metodos {
	
	/*
	 * SINTXE:
	 * 
	 * 		tipo nomeMetodo (parametros caso tenha){
	 * 
	 * 			bloco de códigos
	 * 
	 * 		}
	 * 
	 * <<TIPOS DOS MÉTODOS>>
	 * **Os métodos tem a possibilidade de receber dados (os parametros), retornar dados e 
	 * tambem trabalharem sem receber e nem retornar dados.
	 * 
	 * **O tipo do método indica qual tipo de dado ele irá retornar. Pode ser String, int, double 
	 * entre outros, caso o método não for retornar nenhum dado, colocase o tipo "void" que quer 
	 * dizer vazio.
	 * 
	 * **Dentro de uma classe não é possível ter dois métodos com o mesmo nome 
	 * pedindo os mesmos tipos parâmetros e a mesma quantidade de parâmetros.


	 * <<CONSTRUTORES>>
	 * **O nome do construtor tem que ser igual o nome da classe
	 * **O construtor se diferencia dos métodos pela característica de não ter retorno
	 * sendo assim, não se define um tipo quando vai declarar um construtor.
	 * **O construtor padrão da classe não possui parâmetros.
	 * **Ao criar um novo construtor para utilizar parâmetros, o construtor padrão deixa de existir
	 * para que seja possível utilizar o contrutor padrão (sem parâmetros), é necessário declara-lo 
	 * esplicitamente.
	 */
	Metodos(String nomeMetodo){
		//Construtor exlicitamente definido a qual recebe um parámetro
		//Crontutor padrão deixa de existir
	}
	
	Metodos(){
		//Construtor padrao da classe criado novamente
	} 	
	
	
	/*
	 * 
	 * 
	 * USUÁRIO: 06117383681
	 * SENHA: lopes3fs
	 * ASS ELET.: 554864
	 */
	
	public static void main(String[] args){
		int x;
		x = 1;
		do{
			System.out.print(x +" - ");
			x++;
			}while(x <= 100);
		}
	}
