package classes_metodos;

import classes_metodos.exemplos_classe.Data;

public class ValorNull {
	
		//ERRO DE COMPILAÇÃO: Regra da linguagem violada, o que impede transformar um
	//arquivo .java em um arquivo .class resultando na falha de execução do código.
		//ERRO DE RUNTIME: Algo de errado no código, algo que não impede o código
	//de ser compilado, más o impede de ser executado corretamente. Como por exemplo, 
	//tentar acessar algum atributo de uma variável/objeto null.
	
	public static void main(String[] args) {
		
		/*
		 * >>>>VALOR NULL<<<<
		 * 		Quando atribuimos o valor NULL a uma variável ou objeto, isso significa
		 * que ela não estará referenciando nenhum espaço de memória, portanto, não será
		 * possivel acessár nenhum atributo dela.
		 * 		O valor NULL não é o mesmo que VAZIO.
		 */
		
		String a = null;
		a.concat("!!!");	//Acarretará em um ERRO RUNTIME pois a variável a, está nula.
		
		Data d1 = new Data();
		d1 = null;
		
	}

}
